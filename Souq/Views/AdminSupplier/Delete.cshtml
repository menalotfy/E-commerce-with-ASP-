@*@model Souq.Models.MyClasses.Supplier

    @{
        ViewBag.Title = "Delete";
    }

    <h3 style="color:red; text-align:center;margin-top:50px">Are you sure you want to delete this?</h3>

    <div class="container" style="margin-top:100px">
        <div class="row">


            <div class="col-md-1  p-1">Name</div>
            <div class="col-md-11 p-1" style="font-weight:bold; color:indigo">
                <div> @Html.DisplayFor(model => model.Name)</div>
            </div>

            <div class="col-md-1  p-1">Email</div>
            <div class="col-md-11 p-1" style="font-weight:bold; color:indigo">
                <div> @Html.DisplayFor(model => model.Email)</div>
            </div>

            <div class="col-md-1 p-1">Phone</div>
            <div class="col-md-11 p-1" style="font-weight:bold; color:indigo">
                <div> @Html.DisplayFor(model => model.SupplierPhone)</div>
            </div>

            <div class="col-md-1 p-1">Address</div>
            <div class="col-md-11 p-1" style="font-weight:bold; color:indigo">
                <div> @Html.DisplayFor(model => model.Address)</div>
            </div>
        </div>
    </div>

    <div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" style="margin:2px"/>
            @Html.ActionLink("Cancel", "Index", "AdminSupplier", null, new { @class = "btn btn-primary", style = "margin:2px" })

        </div>
            }
    </div>*@
@model Souq.Models.ApplicationUser

@{
    ViewBag.Title = "Delete";
}

<h3 style="color:red; text-align:center;margin-top:50px">Are you sure you want to delete this?</h3>
<div>

    <div> @Html.DisplayFor(model => model.UserName)</div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
